### Chain if/else


You can of course do this:

~~~~
if (cond) {
  
} else if (cond2) {
  
} else if (cond3) {
  
}
~~~~

However, you can also use a case statement without a parameter:

~~~~
switch {
  case cond:
  // foo
  case cond2:
  // bar
  case cond3:
  // baz
}
~~~~